# ROADMAP.md — VideoAI

> Обновлён 22.06.2026. После пивота план переустроен: старые Спринты 3-5 (визуал Kandinsky, озвучка, монтаж, автопубликация) сняты или отложены, ядро заменено на оживление через fal (Спринт A), дальше — генерация картинок (Спринт B).

## Карта статусов

| Спринт | Тема | Статус |
|---|---|---|
| 0 | Фундамент, лендинг, деплой | ✅ завершён |
| 1 | Авторизация и кабинет | ✅ завершён |
| 2 | Движок задач + первый AI-эндпоинт | ✅ завершён (движок переиспользуется) |
| 3 | Визуал (Kandinsky) + озвучка (SpeechKit) | ⛔ отменён пивотом |
| 4 | Монтаж FFmpeg | ⏸ отложен (вернётся для premium «ролик под ключ») |
| 5 | Автопубликация VK/TG/MAX | ⏸ отложен (вне скоупа MVP, см. `SOCIAL_PROVIDERS.md`) |
| **A** | **Пивот ядра на fal (оживление, эконом Kling)** | ✅ закрыт (утечка устранена, тест №2 пройден) |
| **B** | **Текст → картинка (Nano Banana) → оживление** | ✅ закрыт (сквозной тест пройден 01.06) |
| 6 | Биллинг (ЮMoney-кошелёк) | ✅ закрыт (реальный платёж подтверждён 02.06) |
| **C** | **Редизайн фронтенда (Claude Design) + мерж логики** | ✅ задеплоен (новый B2B-лендинг live, тарифы 3 пакета) |
| — | **Миграция на reg.cloud VPS (РФ)** | ✅ завершена 05.06 (БД в РФ, 152-ФЗ закрыт) |
| — | **Модель доступа + мобильный UX** | ✅ завершён 22.06: WM-гейт, rate-limit, mobile P0 |
| 7 | Админка и предпродакшн | актуален (Блок 1 + Блок 2, ТЗ готовы) |

---

## Спринт 0: фундамент ✅

Лендинг на Railway, домен ddvideoai.ru с HTTPS, структура папок, `theme.js`, `global.css`, `Layout`, `Btn`, маршруты, `/api/health`, статика. **DoD выполнен.**

## Спринт 1: авторизация и кабинет ✅

Таблицы `users` / `auth_codes`, Brevo, OTP, JWT в httpOnly cookie, `LoginPage`, `AuthProvider`, защищённые маршруты, приветственные кредиты (`WELCOME_CREDITS`, дефолт 50), баланс в хедере. **DoD выполнен.**

## Спринт 2: движок задач ✅

`jobs.js` (создание задачи, фон через `setImmediate`, статусы, retry, watchdog, возврат кредитов), таблицы `projects` / `generation_jobs`, API jobs/projects, хук `useJobPolling`. Движок целиком переиспользуется в Спринте A (Wan/Veo — асинхронные submit→poll, ложатся идеально). GigaChat (`llm.js`) сохранён для Спринта B; генерация сценариев — снята. **DoD выполнен.**

## ⛔ Спринт 3: визуал и озвучка — ОТМЕНЁН пивотом

Kandinsky (`image.js`), SpeechKit (`tts.js`), раскадровка, выбор голоса (`voices.js`) — удалены в Спринте A. Yandex Object Storage (S3, `storage.js`) сохранён и используется для хранения фото и готовых клипов.

## ⏸ Спринт 4: монтаж FFmpeg — ОТЛОЖЕН

`editor.js` и шаблоны (`templates.js`) не входят в текущий продукт (1 клип = готовый креатив). Вернётся для premium-фичи «ролик под ключ» (склейка нескольких клипов). FFmpeg на VPS — когда дойдём.

## ⏸ Спринт 5: автопубликация — ОТЛОЖЕН

VK/Telegram/MAX вынесены из MVP. Текущий продукт = пользователь скачивает MP4. Спека сохранена в `SOCIAL_PROVIDERS.md` для возврата на B2B-этапе.

---

## ★ Спринт A: пивот ядра на fal ✅ (закрыт, хвосты)

**Цель:** заменить старый пайплайн на оживление фото в клип через fal.ai.

- [x] Демонтаж старого пайплайна (image.js, tts.js, scenario.js, imagePrompt.js, Storyboard.jsx, voices.js)
- [x] `server/providers/falVideo.js` — Wan 2.7 / Veo 3.1 fast, image-to-video, `@fal-ai/client`, submit→poll→result, перезалив в S3
- [x] Загрузка фото товара: `multer` → S3 (`POST /api/upload`)
- [x] Тип задачи `animate`; free-попытки `free_wan` / `free_veo`
- [x] `GET /api/config` — модели, пресеты движения, цены
- [x] Переработка `EditorPage` (фото → движение → модель → результат) и `ProjectPage` (плеер + скачать)
- [x] Микрофикс параметров fal (720p, аудио выкл., длительность, negative_prompt, enable_prompt_expansion — по-модельно, сверено с доками)
- [x] `FAL_KEY`, `CREDITS_WAN`, `CREDITS_VEO` в Railway
- [x] **Живой тест эконом-модели на проде** — сначала Wan, затем заменён на **Kling 2.5 turbo** ($0.21/ролик, дешевле и стабильнее)
- [x] **Денежная утечка ЗАКРЫТА** — фикс (5 слоёв + reconciler + 404) подтверждён тестами B/C/D **и сквозным тестом №2 на проде** (перезапуск в середине → дочитка без двойного списания, $0.21)
- [x] `negative_prompt` целевой по тексту (в коде)
- [ ] Проверить, поддерживает ли **Kling** `seed` (в Input его нет) — от этого зависит механизм предсказуемости
- [ ] Подтвердить стабильность Kling на 3-4 прогонах (пока 1 удачный)
- [x] Прогон **Veo** на проде (02.06): 8с, 9:16, без аудио, ~$1.20, премиум-качество подтверждено; 9:16 не обрезает товар, достраивает фон
- [ ] Внести переписанные промпты `MOTION_PRESETS` (убрать `rotation`)
- [ ] Обновить лендинг (HomePage) под новый продукт

**DoD:** на проде из реального фото получаются клипы (эконом Kling ✓, Veo — ожидается); списание корректное и без утечки (тест №2 ✓); движение предсказуемое (нужна серия прогонов Kling).

> **Итог тестов:** денежная утечка (fal списывал за зависшие/повторные задачи — на Wan утекло $3 = 6 роликов вместо 1) **устранена и подтверждена**. Эконом-модель переведена Wan → Kling: дешевле ($0.21 vs $0.50) и в первом прогоне стабильнее по тексту/движению. Остаётся подтвердить стабильность Kling серией и вопрос с seed.

## ★ Спринт B: текст → картинка → оживление ✅ ЗАВЕРШЁН (01.06.2026)

**Цель:** если у пользователя нет фото — генерируем картинку по тексту, затем оживляем.

- [x] Починен `llm.js` (удалён мёртвый `scenario.js`)
- [x] `falImage.js`: text-to-image через **Nano Banana 2** ($0.08/картинка, выход 3:4, seed)
- [x] Тип задачи `image` через существующий движок (дедуп/reconciler/возврат); `CREDITS_IMAGE`=13; пробник `free_image`
- [x] GigaChat `buildImagePrompt`: русское описание → английский промпт (с очисткой вывода, проверкой кириллицы)
- [x] `EditorPage`: ветка «нет фото» → форма → картинка → подтверждение → оживление
- [x] Связка картинка → `animate` (Kling/Veo)
- [x] Защиты (B.2): очистка вывода GigaChat, устойчивый парсинг ответа (6 путей), free_image

**DoD выполнен:** сквозной путь описание → GigaChat → Nano Banana → подтверждение → Kling → MP4 пройден живьём. Качество — лучшее за проект (текст на сгенерированной картинке пережил оживление). Суммарная стоимость теста $0.29.

> **Важно для позиционирования:** путь «генерация» создаёт иллюстрацию ПО ОПИСАНИЮ, а не реальный товар пользователя.
> **Хвосты:** пропорции — генерация/Kling 3:4 vs Veo 9:16 (Veo не обрезает, достраивает фон); SSL GigaChat (`rejectUnauthorized:false`); лендинг. (Veo на проде — проверен 02.06.)

---

## Кандидаты (после MVP / отдельные мини-спринты)
- **Выбор длительности ролика (5/10 сек).** НЕ просто кнопка: длина = деньги (10 сек вдвое дороже — Kling $0.42, Veo $2.40), значит цена в кредитах должна зависеть от длины. Допустимые значения у моделей разные и фиксированные (Kling «5»/«10», Veo «4s»/«6s»/«8s» — сверить по схеме). Дольше = выше риск брака текста — тестировать отдельно.

---

## ★ Спринт 6: биллинг (ЮMoney-кошелёк) ✅ ЗАВЕРШЁН (02.06.2026)

`server/payments.js`, таблица `payments`, webhook, `BillingPage`. Провайдер — **ЮMoney-кошелёк** (НЕ ЮKassa): Quickpay-ссылка с меткой `label` → HTTP-уведомление → проверка подписи `sign` (HMAC-SHA256, RFC 3986; НЕ устаревший `sha1_hash`) → идемпотентное начисление по `operation_id` (привязано к `INSERT ... RETURNING`, устойчиво к гонке). `src/data/tariffs.js` — **3 пакета с универсальными кредитами:** Hook Pack (599 ₽ / 120 кр.), Product Shots (1 099 ₽ / 240 кр.), Seller (1 599 ₽ / 360 кр.).

**DoD выполнен:** реальный платёж 199 ₽ прошёл → кошелёк получил 193,03 ₽ (комиссия ЮMoney ~3%) → кредиты начислены → история. Поле суммы в webhook = `amount` (после комиссии); сверка с допуском 10% перекрывает.

> **Хвосты:** email об оплате; polling баланса при `?paid=1`; выбор карта/кошелёк в UI. 54-ФЗ (чеки) — осознанно отложено до оборотов/ИП; миграция на ЮKassa — когда «пойдёт». (`<ErrorBoundary>` уже добавлен 02.06.)

## ★ Спринт C: редизайн фронтенда ✅ ЗАДЕПЛОЕН (03-05.06.2026)

Полная замена фронтенда по макетам Claude Design + мерж бэкенд-логики:
- [x] Новый B2B-лендинг (HomePage): 7 секций, Hero-виджет с демо-видео, адаптив
- [x] Glass-дизайн кабинета (Layout, Dashboard, Editor)
- [x] Тарифы сведены к 3 пакетам (единый источник `tariffs.js`)
- [x] EditorPage: 3-колоночный UI + реальные API (upload, jobs, polling, кредиты/пробники)
- [x] DashboardPage: реальные проекты из API + hover-play + удаление с подтверждением
- [x] ErrorBoundary восстановлен, двойной BrowserRouter исправлен
- [x] AdminRoute (фронт-гейт по role/email через `adminConfig.js`)
- [x] Осиротевшие файлы почищены (ProtectedRoute, GenerationProgress, старый ProjectCard)

## Миграция на reg.cloud VPS (РФ) — ✅ завершена 05.06.2026

Переезд с Railway (США) → reg.cloud VPS (Москва) для локализации ПДн по 152-ФЗ:
- [x] PostgreSQL 18 локально (данные в РФ)
- [x] Nginx reverse-proxy + Let's Encrypt SSL
- [x] PM2 (`ecosystem.config.cjs`, 1 экземпляр, `--env-file=.env`)
- [x] DNS A-записи → новый IP, Cloudflare DNS only
- [x] `trust proxy`, условный SSL в db.js
- [x] Ротация секретов (JWT_SECRET, ключи S3)
- [x] Railway выведен из эксплуатации

## Спринт 7: админка и предпродакшн (актуален, ТЗ готовы)

Разбит на два блока:

**Блок 1 — Админка** (ТЗ `SPRINT_7_BLOCK1_ADMIN_PROMPT.md`, не исполнен):
- Доработка `AdminPage` (убрать мёртвые ветки storyboard/regenerate_scene)
- Мониторинг задач и ошибок по провайдерам, алёрты при failed > порога
- Управление пользователями/кредитами

**Блок 2 — Юр-страницы и правки текстов** (ТЗ `SPRINT_7_BLOCK2_SITE_PROMPT.md`, документы готовы):
- Публикация `/oferta`, `/privacy`, `/consent`
- Правки BillingPage: убрать ложное «выставление чеков»
- 5 прогонов полного пути фото → клип без багов

**DoD:** готовы дать ссылку первым 50 пользователям.

---

## После MVP — приоритеты

1. **Уведомление в Роскомнадзор** (УКЭП через Госключ)
2. **Заявка в ЮKassa** (с офертой) → переписать `payments.js` под ЮKassa (авто-чеки НПД)
3. **Premium «ролик под ключ»** (FFmpeg-склейка, возврат отложенного Спринта 4)
4. **Собственная озвучка** (поздний слой)
5. **Автопубликация в соцсети** (возврат отложенного Спринта 5)
6. **Дальний трек:** уход с реселлера fal на прямой доступ к моделям (−70% себестоимости)
7. Команды/роли, бренд-киты (B2B), аналитика, аватары
8. `EMAIL_FROM` → `noreply@ddvideoai.ru`
9. Рассмотреть ресайз VPS на 2 ГБ RAM при росте нагрузки

---

## Оценка по времени (пересмотр)

Спринты 0-2, A, B, 6, C закрыты. Миграция на VPS (РФ) завершена. Ядро продукта целиком работает вживую: эконом (Kling), премиум (Veo), генерация картинки, приём денег (ЮMoney), B2B-лендинг, новый кабинет. До публичного запуска: Sprint 7 (Блок 1 — админка, Блок 2 — юр-страницы/оферта), мелкие долги (SSL GigaChat, email об оплате). Узкое место — баланс fal, нужно пополнение.
