# Landing Footer Fix Report

## Проблема
В `HomePage.jsx` компонент `FooterCTA` содержал тёмный `<footer>` с битыми ссылками `href="#"` на «Политику конфиденциальности» и «Публичную оферту». Клик прокручивал страницу вверх вместо навигации. Этот footer дублировал правильный `SiteFooter` из `Layout.jsx` (с реквизитами, ИНН и рабочими ссылками).

**Дата:** 07.06.2026
**Статус:** завершён

---

## Решение — Вариант 1

Удалён дублирующий тёмный `<footer>` из `FooterCTA` (строки 388–400). Оставлена CTA-секция «Первый креатив — бесплатно» (баннер с кнопкой). Правильный footer из `Layout.jsx` с реквизитами и рабочими ссылками отображается сразу под CTA.

## Изменённый файл

### `src/pages/HomePage.jsx`
- Удалён блок `<footer style={{ background: '#0A1F16' ... }}>` с:
  - Логотипом + копирайтом `© 2026 · ddvideoai.ru` (дубль Layout)
  - `<a href="#">Политика конфиденциальности</a>` (битая ссылка)
  - `<a href="#">Публичная оферта</a>` (битая ссылка)
- CTA-баннер сохранён без изменений

---

## Тесты

| Тест | Результат |
|---|---|
| `npx vite build` | PASS (1803 модуля, 485 KB JS) |
| `href="#"` в бандле | 0 (удалены) |
| CTA-секция в бандле | Сохранена |

## DoD

- [x] Нет ссылок-заглушек `href="#"` на документы
- [x] Нет двух конфликтующих копирайт-строк (правильный — в Layout)
- [x] Визуал лендинга не сломан (CTA-баннер на месте)
- [x] `npx vite build` — без ошибок
- [x] Данный отчёт создан
